### MYSQL 

* Login 
```mysql
  mysql -u user -p
  # If not running on local host
  mysql -u user -p -h hostName
```  
* Show databases
```mysql
  show databases;
```
* Show tables
```mysql
  show tables;
```
